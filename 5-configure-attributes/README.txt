/*
  CONVERT BETWEEN PROPERTIES AND ATTRIBUTES

  1. OBSERVE an attribute (Set a property from an attribute)
    - The attribute value must be converted from String into the matching data type
      of the property

  2. REFLECT an attribute (Set an attribute from a property)
    - The data type of the property value must be converted to String

    ---------------------

    Instances:

    OBSERVE
    1. attribute --> property = attribute value: Convert from string to right data type

    REFLECT
    2. property --> attribute = Property value must be converted to a string.


*/

